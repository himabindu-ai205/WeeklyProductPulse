# Lightweight deps for the dashboard service on Railway.
# Full pipeline deps stay in requirements.txt (GitHub Actions weekly job).
pydantic>=2.0.0
pyyaml>=6.0
python-dotenv>=1.0.0
# Needed for POST /api/publish-doc (append to Google Doc via MCP)
langchain-mcp-adapters>=0.1.0
mcp>=1.0.0
